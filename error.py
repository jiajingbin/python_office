def passwd_error():
        passwd_error = "-----定位密码输入框超时-----"
        return(passwd_error)
def username_error():
        username_error = "-----定位用户名输入框超时-----"
        return(username_error)
def validatecode_get_error():
        validatecode_get_error = "-----请检查验证码截图-----"
        return(validatecode_get_error)
def validatecode_error():
        validatecode_error = "-----定位验证码输入框超时-----"
        return(validatecode_error)
def login_btn_error():
        login_btn_error = "-----定位登陆按钮超时-----"
        return(login_btn_error)
def after_login():
        after_login = "-----登陆超时，请检查网络或服务状态-----"
        return(after_login)
def login_failure_username():
        login_failure_username = "-----请检查用户名-----"
        return(login_failure_username)
def login_failure_passwd():
        login_failure_passwd = "-----请检查密码-----"
        return(login_failure_passwd)
def login_failure_validatecode():
        login_failure_validatecode = "-----请检查验证码-----"
        return(login_failure_validatecode)
def ip_search_error():
        ip_search_error = "-----检测不到被测终端ip-----"
        return(ip_search_error)
def check_error():
        check_error = "-----检测超时，请手工检查-----"
        return(check_error)
